indexes:
  custom_ft:
    embeddings_path: /fake/path/to/custom_embeddings.csv
    healthcheck_query: health
    model: FooModel
    store_type: MEMORY
models:
  FooModel:
    gcs_folder: fooModelFolder
    score_threshold: 0.5
    type: LOCAL
    usage: EMBEDDINGS
version: 1
