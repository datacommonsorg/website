indexes:
  custom_ft:
    embeddings: /fake/path/to/custom_embeddings.csv
    model: FooModel
    store: MEMORY
models:
  FooModel:
    gcs_folder: fooModelFolder
    score_threshold: 0.5
    type: LOCAL
    usage: EMBEDDINGS
version: 1
